package abstraction;

public interface ParentInterface {//interface created 

	public  void demo() ;//hiding and Abstraction is happening
	
	public int demo1(int a);//Hiding and Abstraction is happening
	
	// no need of using abstract keyword here because interface can have only abstract method.so automatically it is considered as abstract method.
}
